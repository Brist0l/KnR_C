#include<stdio.h>

int main(){
	if (0)
		printf("In If");
	else
		printf("In else");

	if (-100)
		printf("In If");
	else
		printf("In else");
}

// Any non-zero value is true , whilist 0 is always false
